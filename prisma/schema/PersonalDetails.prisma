model PersonalDetails {
    id               String        @id @default(uuid())
    fullName         String
    email            String        @unique
    phoneNumber      String        @unique
    address          String
    dob              DateTime
    emergencyContact String
    language         String?
    nationality      String?
    gender           GenderStatus?
    Staff            Staff[]
}
